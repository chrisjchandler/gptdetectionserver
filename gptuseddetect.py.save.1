import openai

# Set the API key
openai.api_key = "import openai

# Set the API key
openai.api_key = "your_api_key_goes_here"

# Load a pre-trained language model
model = openai.api_key.Model.create(engine="davinci")

# Ask the user to enter the text to classify
text = input("Enter the text to classify: ")

# Define the prompt for the language model
prompt = (
    "Please classify the following text as being generated by OpenAI or not: \n"
    "text: {}".format(text)
)

# Generate a response from the language model
response = model.completion(prompt=prompt, temperature=0.5, max_tokens=1024)

# Print the response
print(response)"

# Load a pre-trained language model
model = openai.api_key.Model.create(engine="davinci")

# Ask the user to enter the text to classify
text = input("Enter the text to classify: ")

# Define the prompt for the language model
prompt = (
    "Please classify the following text as being generated by OpenAI or not: \n"
    "text: {}".format(text)
)

# Generate a response from the language model
response = model.completion(prompt=prompt, temperature=0.5, max_tokens=1024)

# Print the response
print(response)
